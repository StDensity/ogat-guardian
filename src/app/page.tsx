import { ModeToggle } from "@/components/theme/ModeToggle";
import Image from "next/image";

export default function Home() {
   return (
      <div>
         {/* Redirects to front-page Check next.config.ts for more info */}
      </div>
   );
}
